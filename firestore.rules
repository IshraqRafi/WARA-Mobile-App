rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows read and write for authenticated users and during development
      allow read, write: if true;
    }
  }
}
